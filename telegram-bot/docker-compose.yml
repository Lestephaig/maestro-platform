services:
  telegram-bot:
    build: .
    restart: unless-stopped
    init: true
    volumes:
      - ./.env:/app/.env:ro
    healthcheck:
      test: ["CMD", "python", "-m", "maestro_bot.healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
