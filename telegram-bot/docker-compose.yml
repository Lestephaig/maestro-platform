services:
  telegram-bot:
    build: .
    restart: unless-stopped
    init: true
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "python", "-m", "maestro_bot.healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
